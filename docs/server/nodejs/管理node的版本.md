通过N命令来进行node版本的管理：https://www.npmjs.com/package/n

之前是通过nodejs官网下载的安装包，用安装包的方式来安装node的，结果使用了n命令之后，查询不到安装的包，可能是n命令和安装包的路径有什么不同。

所以后来使用n命令重新下载最新版本后，我看/usr/local/bin/下的node文件也替换了，之前还是一个软链接的，现在n管理之后实时替换成一个可执行文件了。

常用命令

```
n ls
查看所有已安装

n
列出所有安装的版本，上下切换选择

n --help
查看命令帮助
```

